name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      LLM_PROVIDER: mock
      IMAGE_PROVIDER: mock
      PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Configure temporary paths
        shell: bash
        run: |
          echo "DATABASE_URL=sqlite:///$RUNNER_TEMP/empty-migration.sqlite" >> "$GITHUB_ENV"
          echo "EXPORT_DIR=$RUNNER_TEMP/exports" >> "$GITHUB_ENV"
          echo "UPLOAD_ROOT=$RUNNER_TEMP/uploads" >> "$GITHUB_ENV"

      - name: Install Python dependencies
        run: uv sync --extra dev

      - name: Upgrade an empty SQLite database
        run: uv run python -m app.migrate

      - name: Ruff
        run: uv run ruff check app tests migrations scripts

      - name: Pytest
        run: uv run python -m pytest -q -p no:cacheprovider

      - name: Deterministic Brief hard-rule evaluation
        run: uv run python -m app.evals.briefs mock-hard

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Build frontend
        working-directory: frontend
        run: npm run build

      - name: Install Playwright Chromium
        working-directory: frontend
        run: npx playwright install --with-deps chromium

      - name: Playwright main flow
        working-directory: frontend
        run: npm run test:e2e
